side_bar_buttons = """
    <style>
      /* Target all buttons inside the sidebar and make them full-width */
      [data-testid="stSidebar"] .stButton > button {
        width: 100% !important;
      }
    </style>
    """